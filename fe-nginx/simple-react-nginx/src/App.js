import React from 'react';

function App() {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      <h1>Welcome to React App deployed with Nginx & Docker!</h1>
      <p>This is a simple React app served by Nginx inside a Docker container.</p>
    </div>
  );
}

export default App;
